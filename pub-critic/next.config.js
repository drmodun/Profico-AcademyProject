/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'pub-critic.netlify.app', "media.rawg.io", "gamesapiandcard-feature-3--pub-critic.netlify.app"],
    },
}

module.exports = nextConfig
